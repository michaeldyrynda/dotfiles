# Agent configs

When asked to run Hari (planning) or Salvor (implementation), read the corresponding file and execute its instructions inline with full tool access. Do not spawn a subagent — read the file and follow it directly.

- Hari (task planner): `~/.claude/agents/task-planner.md`
- Salvor (implementor): `~/.claude/agents/implementor.md`
